## What's Changed
{%    for type_, commits in release["elements"] | dictsort
%}{%  if type_ != "unknown"
%}{{
          "\n### %s\n\n" | format(type_ | title)
}}{%      for commit in commits
%}{{
            "* %s (%s)\n" | format(
              commit.descriptions[0] | capitalize,
              commit.short_hash,
            )

}}{%      endfor
%}{%    endif
%}{%  endfor
%}